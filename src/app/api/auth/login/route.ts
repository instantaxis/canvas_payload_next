import { NextRequest, NextResponse } from "next/server"; export async function POST(req) { const { email, password } = await req.json(); if (!email || !password) { return NextResponse.json({ error: "Invalid inputs" }, { status: 400 }); } if (email === "demo@payload.cms" && password === "Testuser@pwc123") { return NextResponse.json({ success: true, token: "DUMMY_TOKEN" }); } return NextResponse.json({ error: "Unauthorized" }, { status: 401 }); }
